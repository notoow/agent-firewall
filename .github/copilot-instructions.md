# AgentFirewall

Follow `AGENTS.md` for repository instructions.

Before suggesting commands that read secrets, delete files, rewrite git history, run remote scripts, send data externally, or change MCP configuration, describe the risk and prefer an AgentFirewall scan payload.
